# logpulse
A lightweight Python CLI log analyzer. Parses Apache, nginx, and JSON log formats. Generates summary reports with top IPs, URLs, status codes, and traffic patterns. Designed for quick server debugging without heavy infrastructure.
